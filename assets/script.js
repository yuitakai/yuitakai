const translations = {
  ja: {
    navResearch: "Research",
    navPublications: "Publications",
    navCv: "CV",
    navContact: "Contact",
    eyebrow: "Ph.D. student · Soft matter & fluid mechanics",
    heroTitle: "高井 優衣 / Yui Takai",
    heroLead: "疎水性粒子で覆われた液滴「リキッドマーブル」を中心に、液滴・粒子・界面がつくるソフトマターの物理を研究しています。",
    heroPrimary: "研究を見る",
    heroSecondary: "論文を見る",
    profileName: "高井 優衣 / Yui Takai",
    profileRole: "東京大学 Mouterde Lab 博士課程学生",
    profileKeywords: "Liquid marbles · Droplets · Soft matter · Capillarity",
    aboutEyebrow: "About",
    aboutTitle: "研究の背景",
    aboutText1: "私は、疎水性粒子に覆われた液滴「リキッドマーブル」を中心に、液体・粒子・界面がつくる構造と運動を研究しています。",
    aboutText2: "数学、物理、機械工学を横断しながら、身近に見える液滴現象の中にある普遍的な力学を、実験・画像解析・幾何モデルから理解することを目指しています。",
    researchEyebrow: "Research",
    researchTitle: "Research interests",
    research1Title: "Liquid marbles",
    research1Text: "疎水性粒子で覆われた液滴の摩擦、破裂、持ち上げ、脱離を通して、粒子被覆界面の力学を調べています。",
    research2Title: "Droplets & capillarity",
    research2Text: "表面張力、ラプラス圧、メニスカス、濡れを軸に、液滴が形を変え、接触し、動くメカニズムを考えています。",
    research3Title: "Soft interfaces",
    research3Text: "変形する境界、粒子層、粘弾性流体など、やわらかい界面が流れや不安定性に与える影響に興味があります。",
    pubEyebrow: "Publications",
    pubTitle: "Selected publications",
    pub1Authors: "Y. Takai, K. Mukoyama, P. K. Roy, G. Lagubeau, D. Quéré, S. Poincloux, T. Mouterde",
    pub1Venue: "Physical Review Letters, accepted.",
    pub2Authors: "P. K. Roy, Y. Takai, and collaborators",
    pub2Venue: "Nature Communications, accepted.",
    pub3Authors: "P. K. Roy, Y. Takai, and collaborators",
    cvEyebrow: "CV",
    cvTitle: "Biography",
    cv1: "東京大学大学院工学系研究科 機械工学専攻 博士課程。",
    cv2: "お茶の水女子大学大学院 理学専攻 修士課程修了。",
    cv3: "JST SPRING GX、東京大学リーダー博士人材育成基金に採択。",
    cv4: "日本物理学会 学生発表優秀賞。",
    newsEyebrow: "News",
    newsTitle: "News",
    news1: " “Static friction of liquid marbles” が Physical Review Letters に accept されました。",
    news2: " 日本物理学会で学生発表優秀賞を受賞しました。",
    contactEyebrow: "Contact",
    contactTitle: "Contact",
    contactText: "研究、共同研究、セミナー等について、お気軽にご連絡ください。"
  },
  en: {
    navResearch: "Research",
    navPublications: "Publications",
    navCv: "CV",
    navContact: "Contact",
    eyebrow: "Ph.D. student · Soft matter & fluid mechanics",
    heroTitle: "Yui Takai",
    heroLead: "I am a Ph.D. student in the Mouterde Lab at The University of Tokyo. My research explores the soft-matter physics of liquid marbles, droplets, particles, and interfaces.",
    heroPrimary: "View research",
    heroSecondary: "View publications",
    profileName: "Yui Takai",
    profileRole: "Ph.D. student, Mouterde Lab, The University of Tokyo",
    profileKeywords: "Liquid marbles · Droplets · Soft matter · Capillarity",
    aboutEyebrow: "About",
    aboutTitle: "About my research",
    aboutText1: "My research focuses on liquid marbles: droplets coated with hydrophobic particles. I study the structures and dynamics created by liquids, particles, and interfaces.",
    aboutText2: "Combining mathematics, physics, and mechanical engineering, I aim to understand the universal mechanics hidden in familiar droplet phenomena through experiments, image analysis, and geometric modeling.",
    researchEyebrow: "Research",
    researchTitle: "Research interests",
    research1Title: "Liquid marbles",
    research1Text: "I investigate the mechanics of particle-coated interfaces through friction, rupture, lifting, and detachment of liquid marbles.",
    research2Title: "Droplets & capillarity",
    research2Text: "I study how surface tension, Laplace pressure, menisci, and wetting control droplet shape, contact, and motion.",
    research3Title: "Soft interfaces",
    research3Text: "I am interested in how deformable boundaries, particle layers, and viscoelastic fluids affect flows and instabilities.",
    pubEyebrow: "Publications",
    pubTitle: "Selected publications",
    pub1Authors: "Y. Takai, K. Mukoyama, P. K. Roy, G. Lagubeau, D. Quéré, S. Poincloux, T. Mouterde",
    pub1Venue: "Physical Review Letters, accepted.",
    pub2Authors: "P. K. Roy, Y. Takai, and collaborators",
    pub2Venue: "Nature Communications, accepted.",
    pub3Authors: "P. K. Roy, Y. Takai, and collaborators",
    cvEyebrow: "CV",
    cvTitle: "Biography",
    cv1: "Ph.D. student, Department of Mechanical Engineering, The University of Tokyo.",
    cv2: "M.S. in Physics, Ochanomizu University.",
    cv3: "Selected for JST SPRING GX and The University of Tokyo Leader Doctoral Fund.",
    cv4: "Outstanding Student Presentation Award, The Physical Society of Japan.",
    newsEyebrow: "News",
    newsTitle: "News",
    news1: " “Static friction of liquid marbles” was accepted in Physical Review Letters.",
    news2: " Received the Outstanding Student Presentation Award at the Physical Society of Japan.",
    contactEyebrow: "Contact",
    contactTitle: "Get in touch",
    contactText: "Please feel free to contact me about research, collaborations, or seminars."
  },
  fr: {
    navResearch: "Recherche",
    navPublications: "Publications",
    navCv: "CV",
    navContact: "Contact",
    eyebrow: "Doctorante · Matière molle & mécanique des fluides",
    heroTitle: "Yui Takai",
    heroLead: "Je suis doctorante au laboratoire Mouterde à l’Université de Tokyo. Mes recherches portent sur la physique de la matière molle des billes liquides, des gouttes, des particules et des interfaces.",
    heroPrimary: "Voir la recherche",
    heroSecondary: "Voir les publications",
    profileName: "Yui Takai",
    profileRole: "Doctorante, laboratoire Mouterde, Université de Tokyo",
    profileKeywords: "Billes liquides · Gouttes · Matière molle · Capillarité",
    aboutEyebrow: "À propos",
    aboutTitle: "À propos de ma recherche",
    aboutText1: "Mes recherches portent principalement sur les billes liquides : des gouttes recouvertes de particules hydrophobes. J’étudie les structures et les dynamiques créées par les liquides, les particules et les interfaces.",
    aboutText2: "À la croisée des mathématiques, de la physique et du génie mécanique, je cherche à comprendre la mécanique universelle cachée dans des phénomènes de gouttes familiers, à partir d’expériences, d’analyse d’images et de modèles géométriques.",
    researchEyebrow: "Recherche",
    researchTitle: "Thèmes de recherche",
    research1Title: "Billes liquides",
    research1Text: "J’étudie la mécanique des interfaces couvertes de particules à travers le frottement, la rupture, le soulèvement et le détachement des billes liquides.",
    research2Title: "Gouttes & capillarité",
    research2Text: "J’analyse comment la tension de surface, la pression de Laplace, les ménisques et le mouillage contrôlent la forme, le contact et le mouvement des gouttes.",
    research3Title: "Interfaces molles",
    research3Text: "Je m’intéresse à l’influence des frontières déformables, des couches de particules et des fluides viscoélastiques sur les écoulements et les instabilités.",
    pubEyebrow: "Publications",
    pubTitle: "Publications sélectionnées",
    pub1Authors: "Y. Takai, K. Mukoyama, P. K. Roy, G. Lagubeau, D. Quéré, S. Poincloux, T. Mouterde",
    pub1Venue: "Physical Review Letters, accepté.",
    pub2Authors: "P. K. Roy, Y. Takai et collaborateurs",
    pub2Venue: "Nature Communications, accepté.",
    pub3Authors: "P. K. Roy, Y. Takai et collaborateurs",
    cvEyebrow: "CV",
    cvTitle: "Biographie",
    cv1: "Doctorante, Département de génie mécanique, Université de Tokyo.",
    cv2: "Master en physique, Université d’Ochanomizu.",
    cv3: "Sélectionnée pour JST SPRING GX et le Leader Doctoral Fund de l’Université de Tokyo.",
    cv4: "Prix de la meilleure présentation étudiante, Société physique du Japon.",
    newsEyebrow: "Actualités",
    newsTitle: "Actualités",
    news1: " “Static friction of liquid marbles” a été accepté dans Physical Review Letters.",
    news2: " Prix de la meilleure présentation étudiante à la Société physique du Japon.",
    contactEyebrow: "Contact",
    contactTitle: "Contact",
    contactText: "N’hésitez pas à me contacter pour discuter de recherche, de collaborations ou de séminaires."
  }
};

const supportedLangs = Object.keys(translations);
const html = document.documentElement;
const buttons = document.querySelectorAll(".lang-button");
const nodes = document.querySelectorAll("[data-i18n]");

function setLanguage(lang) {
  const next = supportedLangs.includes(lang) ? lang : "ja";
  html.lang = next;
  localStorage.setItem("site-lang", next);

  nodes.forEach((node) => {
    const key = node.dataset.i18n;
    if (translations[next][key]) node.textContent = translations[next][key];
  });

  buttons.forEach((button) => {
    button.classList.toggle("is-active", button.dataset.lang === next);
  });
}

buttons.forEach((button) => {
  button.addEventListener("click", () => setLanguage(button.dataset.lang));
});

const savedLang = localStorage.getItem("site-lang");
const browserLang = navigator.language?.slice(0, 2);
setLanguage(savedLang || (supportedLangs.includes(browserLang) ? browserLang : "ja"));

document.getElementById("year").textContent = new Date().getFullYear();

const revealObserver = new IntersectionObserver((entries) => {
  entries.forEach((entry) => {
    if (entry.isIntersecting) {
      entry.target.classList.add("is-visible");
      revealObserver.unobserve(entry.target);
    }
  });
}, { threshold: 0.12 });

document.querySelectorAll(".reveal").forEach((el) => revealObserver.observe(el));

/* ============================================================
   Physical water refraction ripple v8 — viewport-locked version

   Why v8 is different:
   - v6 used a full-document screenshot and then converted viewport pixels
     to document pixels. That can fail when the captured full page is scaled,
     clipped, or rendered differently by the browser.
   - v8 captures exactly the current viewport and maps canvas pixels directly
     to screenshot pixels. No document-coordinate conversion is used during
     refraction, so the clicked position and sampled text stay aligned.

   Visual rules:
   - The DOM itself never moves.
   - No white drawn rings.
   - No cloned objects appearing/disappearing.
   - Only the wave band is drawn on a transparent canvas.
   - Inside the wave band, pixels are taken from a displaced screenshot of
     the visible page, so text/cards/photos behind the ripple bend.
   - Shadow/highlight colours are derived from the local background pixel.
   ============================================================ */

const waterCanvas = document.getElementById("waterRefraction");
const waterCtx = waterCanvas?.getContext("2d", { alpha: true, willReadFrequently: true });

const waterSettings = {
  renderScale: Math.min(window.devicePixelRatio || 1, 1.25),

  // Visible but not violent. Increase strength to 34–38 if you want a stronger lens.
  strength: 30,
  tangentialStrength: 1.15,
  shadowStrength: 0.24,
  highlightStrength: 0.055,

  duration: 1500,
  maxRadius: 175,
  wavelength: 15,
  ringSigma: 8.5,
  tailRings: 5,

  // Pixels below this wave energy are fully transparent. This removes square artifacts.
  visibilityCutoff: 0.018,
  captureDebounce: 180
};

let snapshotCanvas = null;
let snapshotCtx = null;
let snapshotImageData = null;
let snapshotKey = "";
let isCapturing = false;
let captureQueued = false;
let ripples = [];
let animationRunning = false;
let resizeTimer = 0;
let snapshotTimer = 0;
let scrollTimer = 0;

function viewportKey() {
  return [
    Math.round(window.innerWidth),
    Math.round(window.innerHeight),
    Math.round(window.scrollX || 0),
    Math.round(window.scrollY || 0),
    document.documentElement.lang || "ja",
    // Small key that changes when the hero image source changes.
    document.querySelector(".hero-photo img")?.currentSrc || ""
  ].join(":");
}

function resizeWaterCanvas() {
  if (!waterCanvas || !waterCtx) return;
  const s = waterSettings.renderScale;
  const w = Math.max(1, Math.round(window.innerWidth * s));
  const h = Math.max(1, Math.round(window.innerHeight * s));
  if (waterCanvas.width !== w || waterCanvas.height !== h) {
    waterCanvas.width = w;
    waterCanvas.height = h;
    waterCanvas.style.width = `${window.innerWidth}px`;
    waterCanvas.style.height = `${window.innerHeight}px`;
  }
}

function markSnapshotStale() {
  snapshotKey = "";
}

function clamp(value, min, max) {
  return Math.max(min, Math.min(max, value));
}

function smoothstep(edge0, edge1, x) {
  const t = clamp((x - edge0) / (edge1 - edge0), 0, 1);
  return t * t * (3 - 2 * t);
}

function easeOutCubic(t) {
  return 1 - Math.pow(1 - clamp(t, 0, 1), 3);
}

function sampleBilinear(data, width, height, x, y) {
  x = clamp(x, 0, width - 1.001);
  y = clamp(y, 0, height - 1.001);

  const x0 = Math.floor(x);
  const y0 = Math.floor(y);
  const x1 = Math.min(width - 1, x0 + 1);
  const y1 = Math.min(height - 1, y0 + 1);
  const tx = x - x0;
  const ty = y - y0;

  const i00 = (y0 * width + x0) * 4;
  const i10 = (y0 * width + x1) * 4;
  const i01 = (y1 * width + x0) * 4;
  const i11 = (y1 * width + x1) * 4;

  const out = [0, 0, 0, 255];
  for (let c = 0; c < 4; c += 1) {
    const a = data[i00 + c] * (1 - tx) + data[i10 + c] * tx;
    const b = data[i01 + c] * (1 - tx) + data[i11 + c] * tx;
    out[c] = a * (1 - ty) + b * ty;
  }
  return out;
}

async function captureViewport() {
  if (!waterCanvas || !waterCtx || !window.html2canvas) return null;

  if (isCapturing) {
    captureQueued = true;
    return snapshotCanvas;
  }

  isCapturing = true;
  const currentKey = viewportKey();

  try {
    // Hide only the overlay. The real page stays in place and is captured as seen.
    const previousVisibility = waterCanvas.style.visibility;
    waterCanvas.style.visibility = "hidden";
    await new Promise((resolve) => requestAnimationFrame(resolve));

    const captured = await window.html2canvas(document.body, {
      backgroundColor: null,
      scale: waterSettings.renderScale,
      width: window.innerWidth,
      height: window.innerHeight,
      x: window.scrollX || 0,
      y: window.scrollY || 0,
      scrollX: window.scrollX || 0,
      scrollY: window.scrollY || 0,
      windowWidth: window.innerWidth,
      windowHeight: window.innerHeight,
      ignoreElements: (el) => el === waterCanvas || el.classList?.contains("water-refraction"),
      logging: false,
      useCORS: true,
      allowTaint: false,
      removeContainer: true
    });

    waterCanvas.style.visibility = previousVisibility;

    snapshotCanvas = captured;
    snapshotCtx = captured.getContext("2d", { willReadFrequently: true });
    snapshotImageData = snapshotCtx.getImageData(0, 0, captured.width, captured.height);
    snapshotKey = currentKey;
    return snapshotCanvas;
  } catch (error) {
    waterCanvas.style.visibility = "";
    snapshotCanvas = null;
    snapshotCtx = null;
    snapshotImageData = null;
    snapshotKey = "";
    console.warn("Water ripple capture failed. The ripple will fall back to a soft local shadow.", error);
    return null;
  } finally {
    isCapturing = false;
    if (captureQueued) {
      captureQueued = false;
      window.setTimeout(() => captureViewport(), 100);
    }
  }
}

function computeLocalShade(sampled, base, slope, life) {
  // Negative slope becomes a soft shadow; positive slope becomes a very weak highlight.
  const shade = clamp(slope * waterSettings.shadowStrength * life, -0.52, 0.28);
  let r = sampled[0];
  let g = sampled[1];
  let b = sampled[2];

  const baseLuma = 0.2126 * base[0] + 0.7152 * base[1] + 0.0722 * base[2];

  if (shade < 0) {
    const dark = -shade;
    const desat = 0.06 + 0.14 * dark;
    r = r * (1 - desat) + baseLuma * desat;
    g = g * (1 - desat) + baseLuma * desat;
    b = b * (1 - desat) + baseLuma * desat;
    r *= 1 - 0.55 * dark;
    g *= 1 - 0.53 * dark;
    b *= 1 - 0.50 * dark;
  } else {
    const light = shade * waterSettings.highlightStrength;
    r = r + (255 - r) * light;
    g = g + (255 - g) * light;
    b = b + (255 - b) * light;
  }

  return [clamp(r, 0, 255), clamp(g, 0, 255), clamp(b, 0, 255)];
}

function drawShadowOnly(ctx, ripple, now) {
  const s = waterSettings.renderScale;
  const age = now - ripple.start;
  const progress = clamp(age / waterSettings.duration, 0, 1);
  const life = Math.pow(1 - smoothstep(0.5, 1, progress), 2) * smoothstep(0, 0.08, progress);
  if (life < 0.03) return progress < 1;

  const front = waterSettings.maxRadius * easeOutCubic(progress) * s;
  const cx = ripple.x * s;
  const cy = ripple.y * s;
  ctx.save();
  ctx.globalAlpha = 0.12 * life;
  ctx.strokeStyle = "rgba(26, 48, 58, 0.35)";
  ctx.lineWidth = 1.1 * s;
  for (let n = 0; n < waterSettings.tailRings; n += 1) {
    const r = front - n * waterSettings.wavelength * 0.92 * s;
    if (r > 4) {
      ctx.beginPath();
      ctx.arc(cx, cy, r, 0, Math.PI * 2);
      ctx.stroke();
    }
  }
  ctx.restore();
  return progress < 1;
}

function drawRefractedRipple(ctx, ripple, now) {
  const s = waterSettings.renderScale;
  const age = now - ripple.start;
  const progress = clamp(age / waterSettings.duration, 0, 1);
  if (progress >= 1) return false;

  const life = Math.pow(1 - smoothstep(0.58, 1, progress), 2) * smoothstep(0, 0.05, progress);
  if (life < 0.016 && progress > 0.72) return false;

  const W = waterCanvas.width;
  const H = waterCanvas.height;
  const cx = ripple.x * s;
  const cy = ripple.y * s;
  const maxR = waterSettings.maxRadius * s;
  const front = maxR * easeOutCubic(progress);
  const tail = (waterSettings.tailRings - 1) * waterSettings.wavelength * 0.92 * s;
  const outerR = Math.min(maxR + 3 * waterSettings.ringSigma * s, front + 3.4 * waterSettings.ringSigma * s);
  const innerR = Math.max(0, front - tail - 3.4 * waterSettings.ringSigma * s);

  const x0 = Math.max(0, Math.floor(cx - outerR - 4));
  const y0 = Math.max(0, Math.floor(cy - outerR - 4));
  const x1 = Math.min(W, Math.ceil(cx + outerR + 4));
  const y1 = Math.min(H, Math.ceil(cy + outerR + 4));
  const bw = x1 - x0;
  const bh = y1 - y0;
  if (bw <= 0 || bh <= 0) return true;

  if (!snapshotImageData || snapshotImageData.width < 10 || snapshotImageData.height < 10) {
    return drawShadowOnly(ctx, ripple, now);
  }

  const src = snapshotImageData.data;
  const srcW = snapshotImageData.width;
  const srcH = snapshotImageData.height;
  const outImage = ctx.createImageData(bw, bh);
  const out = outImage.data;

  const wavelength = waterSettings.wavelength * s;
  const sigma = waterSettings.ringSigma * s;
  const k = (Math.PI * 2) / wavelength;

  for (let py = 0; py < bh; py += 1) {
    const y = y0 + py;
    const dy = y - cy;
    for (let px = 0; px < bw; px += 1) {
      const x = x0 + px;
      const dx = x - cx;
      const r = Math.sqrt(dx * dx + dy * dy);
      const outIndex = (py * bw + px) * 4;

      if (r < 1 || r < innerR || r > outerR) {
        out[outIndex + 3] = 0;
        continue;
      }

      let height = 0;
      let slope = 0;
      let energy = 0;

      for (let n = 0; n < waterSettings.tailRings; n += 1) {
        const ringR = front - n * wavelength * 0.92;
        if (ringR <= 0) continue;
        const local = r - ringR;
        const envelope = Math.exp(-(local * local) / (2 * sigma * sigma));
        const decay = Math.pow(0.68, n);
        const phase = local * k;
        height += Math.sin(phase) * envelope * decay;
        slope += Math.cos(phase) * envelope * decay;
        energy += envelope * decay;
      }

      const visibility = energy * life;
      if (visibility < waterSettings.visibilityCutoff) {
        out[outIndex + 3] = 0;
        continue;
      }

      const nx = dx / r;
      const ny = dy / r;
      const alphaShape = smoothstep(waterSettings.visibilityCutoff, 0.18, visibility);
      const replacementAlpha = clamp(alphaShape * (0.995 - 0.1 * progress), 0, 0.995);

      const localSlope = clamp(slope, -1.2, 1.2);
      const localHeight = clamp(height, -1.25, 1.25);
      const radialDisplacement = waterSettings.strength * localSlope * life * alphaShape;
      const tangentialDisplacement = waterSettings.tangentialStrength * localHeight * life * alphaShape;

      // Direct viewport mapping: overlay canvas pixel coordinates and snapshot
      // pixel coordinates are in the same coordinate system. This removes the
      // coordinate conversion that caused v6 to miss or misplace distorted text.
      const sampleX = x - nx * radialDisplacement - ny * tangentialDisplacement;
      const sampleY = y - ny * radialDisplacement + nx * tangentialDisplacement;

      const sampled = sampleBilinear(src, srcW, srcH, sampleX, sampleY);
      const base = sampleBilinear(src, srcW, srcH, x, y);
      const shaded = computeLocalShade(sampled, base, slope, life * alphaShape);

      out[outIndex] = shaded[0];
      out[outIndex + 1] = shaded[1];
      out[outIndex + 2] = shaded[2];
      out[outIndex + 3] = Math.round(replacementAlpha * 255);
    }
  }

  ctx.putImageData(outImage, x0, y0);
  return true;
}

function animateWater(now) {
  if (!waterCanvas || !waterCtx) return;
  resizeWaterCanvas();
  waterCtx.clearRect(0, 0, waterCanvas.width, waterCanvas.height);

  const stillAlive = [];
  for (const ripple of ripples) {
    if (drawRefractedRipple(waterCtx, ripple, now)) stillAlive.push(ripple);
  }
  ripples = stillAlive;

  if (ripples.length > 0) {
    requestAnimationFrame(animateWater);
  } else {
    animationRunning = false;
    waterCtx.clearRect(0, 0, waterCanvas.width, waterCanvas.height);
  }
}

function startWaterRippleAt(clientX, clientY) {
  ripples.push({
    x: clientX,
    y: clientY,
    start: performance.now()
  });

  if (!animationRunning) {
    animationRunning = true;
    requestAnimationFrame(animateWater);
  }
}

async function addWaterRipple(event) {
  if (!waterCanvas || !waterCtx) return;
  if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return;
  if (event.metaKey || event.ctrlKey || event.shiftKey || event.altKey) return;

  resizeWaterCanvas();

  const key = viewportKey();
  if (!snapshotCanvas || snapshotKey !== key) {
    await captureViewport();
  }

  startWaterRippleAt(event.clientX, event.clientY);
}

function scheduleSnapshot(delay = waterSettings.captureDebounce) {
  markSnapshotStale();
  window.clearTimeout(snapshotTimer);
  snapshotTimer = window.setTimeout(() => {
    if (document.visibilityState === "visible") captureViewport();
  }, delay);
}

resizeWaterCanvas();

window.addEventListener("load", () => {
  window.setTimeout(async () => {
    await captureViewport();
    startWaterRippleAt(window.innerWidth * 0.5, window.innerHeight * 0.42);
  }, 700);
});

window.addEventListener("resize", () => {
  window.clearTimeout(resizeTimer);
  resizeTimer = window.setTimeout(() => {
    resizeWaterCanvas();
    scheduleSnapshot(160);
  }, 140);
});

window.addEventListener("scroll", () => {
  window.clearTimeout(scrollTimer);
  scrollTimer = window.setTimeout(() => scheduleSnapshot(60), 90);
}, { passive: true });

document.addEventListener("visibilitychange", () => {
  if (document.visibilityState === "visible") scheduleSnapshot(140);
});

buttons.forEach((button) => {
  button.addEventListener("click", () => window.setTimeout(() => scheduleSnapshot(80), 90));
});

// Late recapture after fonts/images finish settling.
window.setTimeout(() => scheduleSnapshot(120), 1200);
document.addEventListener("click", addWaterRipple, { passive: true });
