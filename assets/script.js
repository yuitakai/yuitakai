const translations = {
  ja: {
    navResearch: "Research",
    navPublications: "Publications",
    navCv: "CV",
    navContact: "Contact",
    eyebrow: "Ph.D. student · Soft matter & fluid mechanics",
    heroTitle: "液滴の表面に現れる、やわらかな物理を探る。",
    heroLead: "東京大学大学院工学系研究科で、リキッドマーブル、液滴、粒子被覆界面、ソフトマターの力学を研究しています。",
    heroPrimary: "研究を見る",
    heroSecondary: "論文を見る",
    profileName: "高井 優衣 / Yui Takai",
    profileRole: "東京大学 Mouterde Lab 博士課程学生",
    profileKeywords: "Liquid marbles · Droplets · Soft matter · Capillarity",
    aboutEyebrow: "About",
    aboutTitle: "研究の背景",
    aboutText1: "私は、疎水性粒子に覆われた液滴「リキッドマーブル」を中心に、液体・粒子・界面がつくる構造と運動を研究しています。",
    aboutText2: "数学、物理、機械工学を横断しながら、身近に見える液滴現象の中にある普遍的な力学を、実験・画像解析・幾何モデルから理解することを目指しています。",
    researchEyebrow: "Research",
    researchTitle: "Research interests",
    research1Title: "Liquid marbles",
    research1Text: "疎水性粒子で覆われた液滴の摩擦、破裂、持ち上げ、脱離を通して、粒子被覆界面の力学を調べています。",
    research2Title: "Droplets & capillarity",
    research2Text: "表面張力、ラプラス圧、メニスカス、濡れを軸に、液滴が形を変え、接触し、動くメカニズムを考えています。",
    research3Title: "Soft interfaces",
    research3Text: "変形する境界、粒子層、粘弾性流体など、やわらかい界面が流れや不安定性に与える影響に興味があります。",
    pubEyebrow: "Publications",
    pubTitle: "Selected publications",
    pub1Authors: "Y. Takai, K. Mukoyama, P. K. Roy, G. Lagubeau, D. Quéré, S. Poincloux, T. Mouterde",
    pub1Venue: "Physical Review Letters, accepted.",
    pub2Authors: "P. K. Roy, Y. Takai, and collaborators",
    pub2Venue: "Nature Communications, accepted.",
    pub3Authors: "P. K. Roy, Y. Takai, and collaborators",
    cvEyebrow: "CV",
    cvTitle: "Biography",
    cv1: "東京大学大学院工学系研究科 機械工学専攻 博士課程。",
    cv2: "お茶の水女子大学大学院 理学専攻 修士課程修了。",
    cv3: "JST SPRING GX、東京大学リーダー博士人材育成基金に採択。",
    cv4: "日本物理学会 学生発表優秀賞。",
    newsEyebrow: "News",
    newsTitle: "News",
    news1: " “Static friction of liquid marbles” が Physical Review Letters に accept されました。",
    news2: " 日本物理学会で学生発表優秀賞を受賞しました。",
    contactEyebrow: "Contact",
    contactTitle: "Contact",
    contactText: "研究、共同研究、セミナー等について、お気軽にご連絡ください。",
    footerText: "Light-blue water-inspired design."
  },
  en: {
    navResearch: "Research",
    navPublications: "Publications",
    navCv: "CV",
    navContact: "Contact",
    eyebrow: "Ph.D. student · Soft matter & fluid mechanics",
    heroTitle: "Exploring soft physics at liquid interfaces.",
    heroLead: "I am a Ph.D. student at The University of Tokyo studying liquid marbles, droplets, particle-coated interfaces, and soft-matter mechanics.",
    heroPrimary: "View research",
    heroSecondary: "View publications",
    profileName: "Yui Takai",
    profileRole: "Ph.D. student, Mouterde Lab, The University of Tokyo",
    profileKeywords: "Liquid marbles · Droplets · Soft matter · Capillarity",
    aboutEyebrow: "About",
    aboutTitle: "About my research",
    aboutText1: "My research focuses on liquid marbles: droplets coated with hydrophobic particles. I study the structures and dynamics created by liquids, particles, and interfaces.",
    aboutText2: "Combining mathematics, physics, and mechanical engineering, I aim to understand the universal mechanics hidden in familiar droplet phenomena through experiments, image analysis, and geometric modeling.",
    researchEyebrow: "Research",
    researchTitle: "Research interests",
    research1Title: "Liquid marbles",
    research1Text: "I investigate the mechanics of particle-coated interfaces through friction, rupture, lifting, and detachment of liquid marbles.",
    research2Title: "Droplets & capillarity",
    research2Text: "I study how surface tension, Laplace pressure, menisci, and wetting control droplet shape, contact, and motion.",
    research3Title: "Soft interfaces",
    research3Text: "I am interested in how deformable boundaries, particle layers, and viscoelastic fluids affect flows and instabilities.",
    pubEyebrow: "Publications",
    pubTitle: "Selected publications",
    pub1Authors: "Y. Takai, K. Mukoyama, P. K. Roy, G. Lagubeau, D. Quéré, S. Poincloux, T. Mouterde",
    pub1Venue: "Physical Review Letters, accepted.",
    pub2Authors: "P. K. Roy, Y. Takai, and collaborators",
    pub2Venue: "Nature Communications, accepted.",
    pub3Authors: "P. K. Roy, Y. Takai, and collaborators",
    cvEyebrow: "CV",
    cvTitle: "Biography",
    cv1: "Ph.D. student, Department of Mechanical Engineering, The University of Tokyo.",
    cv2: "M.S. in Physics, Ochanomizu University.",
    cv3: "Selected for JST SPRING GX and The University of Tokyo Leader Doctoral Fund.",
    cv4: "Outstanding Student Presentation Award, The Physical Society of Japan.",
    newsEyebrow: "News",
    newsTitle: "News",
    news1: " “Static friction of liquid marbles” was accepted in Physical Review Letters.",
    news2: " Received the Outstanding Student Presentation Award at the Physical Society of Japan.",
    contactEyebrow: "Contact",
    contactTitle: "Get in touch",
    contactText: "Please feel free to contact me about research, collaborations, or seminars.",
    footerText: "Built with a light-blue water-inspired design."
  },
  fr: {
    navResearch: "Recherche",
    navPublications: "Publications",
    navCv: "CV",
    navContact: "Contact",
    eyebrow: "Doctorante · Matière molle & mécanique des fluides",
    heroTitle: "Explorer la physique douce aux interfaces liquides.",
    heroLead: "Je suis doctorante à l’Université de Tokyo et j’étudie les billes liquides, les gouttes, les interfaces couvertes de particules et la mécanique de la matière molle.",
    heroPrimary: "Voir la recherche",
    heroSecondary: "Voir les publications",
    profileName: "Yui Takai",
    profileRole: "Doctorante, laboratoire Mouterde, Université de Tokyo",
    profileKeywords: "Billes liquides · Gouttes · Matière molle · Capillarité",
    aboutEyebrow: "À propos",
    aboutTitle: "À propos de ma recherche",
    aboutText1: "Mes recherches portent principalement sur les billes liquides : des gouttes recouvertes de particules hydrophobes. J’étudie les structures et les dynamiques créées par les liquides, les particules et les interfaces.",
    aboutText2: "À la croisée des mathématiques, de la physique et du génie mécanique, je cherche à comprendre la mécanique universelle cachée dans des phénomènes de gouttes familiers, à partir d’expériences, d’analyse d’images et de modèles géométriques.",
    researchEyebrow: "Recherche",
    researchTitle: "Thèmes de recherche",
    research1Title: "Billes liquides",
    research1Text: "J’étudie la mécanique des interfaces couvertes de particules à travers le frottement, la rupture, le soulèvement et le détachement des billes liquides.",
    research2Title: "Gouttes & capillarité",
    research2Text: "J’analyse comment la tension de surface, la pression de Laplace, les ménisques et le mouillage contrôlent la forme, le contact et le mouvement des gouttes.",
    research3Title: "Interfaces molles",
    research3Text: "Je m’intéresse à l’influence des frontières déformables, des couches de particules et des fluides viscoélastiques sur les écoulements et les instabilités.",
    pubEyebrow: "Publications",
    pubTitle: "Publications sélectionnées",
    pub1Authors: "Y. Takai, K. Mukoyama, P. K. Roy, G. Lagubeau, D. Quéré, S. Poincloux, T. Mouterde",
    pub1Venue: "Physical Review Letters, accepté.",
    pub2Authors: "P. K. Roy, Y. Takai et collaborateurs",
    pub2Venue: "Nature Communications, accepté.",
    pub3Authors: "P. K. Roy, Y. Takai et collaborateurs",
    cvEyebrow: "CV",
    cvTitle: "Biographie",
    cv1: "Doctorante, Département de génie mécanique, Université de Tokyo.",
    cv2: "Master en physique, Université d’Ochanomizu.",
    cv3: "Sélectionnée pour JST SPRING GX et le Leader Doctoral Fund de l’Université de Tokyo.",
    cv4: "Prix de la meilleure présentation étudiante, Société physique du Japon.",
    newsEyebrow: "Actualités",
    newsTitle: "Actualités",
    news1: " “Static friction of liquid marbles” a été accepté dans Physical Review Letters.",
    news2: " Prix de la meilleure présentation étudiante à la Société physique du Japon.",
    contactEyebrow: "Contact",
    contactTitle: "Contact",
    contactText: "N’hésitez pas à me contacter pour discuter de recherche, de collaborations ou de séminaires.",
    footerText: "Design bleu clair inspiré de l’eau."
  }
};

const supportedLangs = Object.keys(translations);
const html = document.documentElement;
const buttons = document.querySelectorAll(".lang-button");
const nodes = document.querySelectorAll("[data-i18n]");

function setLanguage(lang) {
  const next = supportedLangs.includes(lang) ? lang : "ja";
  html.lang = next;
  localStorage.setItem("site-lang", next);

  nodes.forEach((node) => {
    const key = node.dataset.i18n;
    if (translations[next][key]) node.textContent = translations[next][key];
  });

  buttons.forEach((button) => {
    button.classList.toggle("is-active", button.dataset.lang === next);
  });
}

buttons.forEach((button) => {
  button.addEventListener("click", () => setLanguage(button.dataset.lang));
});

const savedLang = localStorage.getItem("site-lang");
const browserLang = navigator.language?.slice(0, 2);
setLanguage(savedLang || (supportedLangs.includes(browserLang) ? browserLang : "ja"));

document.getElementById("year").textContent = new Date().getFullYear();

const revealObserver = new IntersectionObserver((entries) => {
  entries.forEach((entry) => {
    if (entry.isIntersecting) {
      entry.target.classList.add("is-visible");
      revealObserver.unobserve(entry.target);
    }
  });
}, { threshold: 0.12 });

document.querySelectorAll(".reveal").forEach((el) => revealObserver.observe(el));

/* ============================================================
   Physically inspired water refraction ripple

   Goal:
   - No white rings.
   - No object appearing/disappearing.
   - No page-wide movement.
   - The visible page is captured to an offscreen canvas, then only the
     wave region is re-sampled with a radial displacement field.
   - Grey shading is added from the wave slope to express concave/convex relief.

   This uses html2canvas when available. If the CDN fails, it still draws
   subtle grey wave shadows, but true text/image refraction needs the capture.
   ============================================================ */

const waterCanvas = document.getElementById("waterRefraction");
const waterCtx = waterCanvas?.getContext("2d", { alpha: true, willReadFrequently: true });

const waterSettings = {
  // Keep this near 1.0 for stable performance on GitHub Pages and laptops.
  renderScale: Math.min(window.devicePixelRatio || 1, 1.18),

  // Visual tuning. Increase strength slightly if the distortion is too weak.
  strength: 14,
  shadowStrength: 0.34,
  duration: 2600,
  maxRadius: 520,
  wavelength: 26,
  ringWidth: 42,
  speed: 360,
  tailRings: 4
};

let snapshotCanvas = null;
let snapshotCtx = null;
let snapshotKey = "";
let isCapturing = false;
let captureQueued = false;
let ripples = [];
let animationRunning = false;
let resizeTimer = 0;
let scrollTimer = 0;

function viewportKey() {
  return [
    Math.round(window.innerWidth),
    Math.round(window.innerHeight),
    Math.round(window.scrollX),
    Math.round(window.scrollY),
    document.documentElement.lang || "ja"
  ].join(":");
}

function resizeWaterCanvas() {
  if (!waterCanvas || !waterCtx) return;
  const s = waterSettings.renderScale;
  const w = Math.max(1, Math.round(window.innerWidth * s));
  const h = Math.max(1, Math.round(window.innerHeight * s));
  if (waterCanvas.width !== w || waterCanvas.height !== h) {
    waterCanvas.width = w;
    waterCanvas.height = h;
    waterCanvas.style.width = `${window.innerWidth}px`;
    waterCanvas.style.height = `${window.innerHeight}px`;
  }
}

function markSnapshotStale() {
  snapshotKey = "";
}

async function captureViewport() {
  if (!waterCanvas || !waterCtx) return null;

  // Avoid concurrent captures. If one is already running, remember that
  // another capture may be needed after it finishes.
  if (isCapturing) {
    captureQueued = true;
    return snapshotCanvas;
  }

  if (!window.html2canvas) {
    return null;
  }

  isCapturing = true;
  const currentKey = viewportKey();

  try {
    // Hide the overlay during capture so the screenshot is the real page only.
    const previousOpacity = waterCanvas.style.opacity;
    const previousDisplay = waterCanvas.style.display;
    waterCanvas.style.opacity = "0";
    waterCanvas.style.display = "none";

    // Let style changes apply before capture.
    await new Promise((resolve) => requestAnimationFrame(resolve));

    const captured = await window.html2canvas(document.body, {
      backgroundColor: null,
      scale: waterSettings.renderScale,
      width: window.innerWidth,
      height: window.innerHeight,
      x: window.scrollX,
      y: window.scrollY,
      scrollX: window.scrollX,
      scrollY: window.scrollY,
      windowWidth: document.documentElement.scrollWidth,
      windowHeight: document.documentElement.scrollHeight,
      ignoreElements: (el) => el === waterCanvas || el.classList?.contains("water-refraction"),
      logging: false,
      useCORS: true,
      allowTaint: false
    });

    waterCanvas.style.display = previousDisplay;
    waterCanvas.style.opacity = previousOpacity;

    snapshotCanvas = captured;
    snapshotCtx = captured.getContext("2d", { willReadFrequently: true });
    snapshotKey = currentKey;
    return snapshotCanvas;
  } catch (error) {
    // Restore overlay even if capture fails.
    waterCanvas.style.display = "";
    waterCanvas.style.opacity = "";
    console.warn("Water ripple capture failed; falling back to shadow-only waves.", error);
    return null;
  } finally {
    isCapturing = false;
    if (captureQueued) {
      captureQueued = false;
      window.setTimeout(() => captureViewport(), 120);
    }
  }
}

function clamp(value, min, max) {
  return Math.max(min, Math.min(max, value));
}

function smoothstep(edge0, edge1, x) {
  const t = clamp((x - edge0) / (edge1 - edge0), 0, 1);
  return t * t * (3 - 2 * t);
}

function sampleBilinear(data, width, height, x, y) {
  x = clamp(x, 0, width - 1.001);
  y = clamp(y, 0, height - 1.001);

  const x0 = Math.floor(x);
  const y0 = Math.floor(y);
  const x1 = Math.min(width - 1, x0 + 1);
  const y1 = Math.min(height - 1, y0 + 1);
  const tx = x - x0;
  const ty = y - y0;

  const i00 = (y0 * width + x0) * 4;
  const i10 = (y0 * width + x1) * 4;
  const i01 = (y1 * width + x0) * 4;
  const i11 = (y1 * width + x1) * 4;

  const out = [0, 0, 0, 0];
  for (let c = 0; c < 4; c += 1) {
    const a = data[i00 + c] * (1 - tx) + data[i10 + c] * tx;
    const b = data[i01 + c] * (1 - tx) + data[i11 + c] * tx;
    out[c] = a * (1 - ty) + b * ty;
  }
  return out;
}

function drawShadowOnly(ctx, ripple, age, progress) {
  const s = waterSettings.renderScale;
  const radius = Math.min(waterSettings.maxRadius, waterSettings.speed * age / 1000) * s;
  const cx = ripple.x * s;
  const cy = ripple.y * s;
  const alpha = (1 - progress) * 0.18;

  ctx.save();
  ctx.globalAlpha = alpha;
  ctx.lineWidth = Math.max(1, 1.4 * s);
  ctx.strokeStyle = "rgba(54, 71, 80, 0.42)";
  for (let i = 0; i < 4; i += 1) {
    const r = radius - i * waterSettings.wavelength * 1.1 * s;
    if (r > 4) {
      ctx.beginPath();
      ctx.arc(cx, cy, r, 0, Math.PI * 2);
      ctx.stroke();
    }
  }
  ctx.restore();
}

function drawRefractedRipple(ctx, ripple, now) {
  const s = waterSettings.renderScale;
  const age = now - ripple.start;
  const progress = clamp(age / waterSettings.duration, 0, 1);
  if (progress >= 1) return false;

  const W = waterCanvas.width;
  const H = waterCanvas.height;
  const cx = ripple.x * s;
  const cy = ripple.y * s;
  const maxR = waterSettings.maxRadius * s;
  const front = Math.min(maxR, (waterSettings.speed * age / 1000 + 16) * s);
  const tail = waterSettings.tailRings * waterSettings.wavelength * s;
  const boundR = Math.min(maxR, front + waterSettings.ringWidth * s);
  const innerR = Math.max(0, front - tail - waterSettings.ringWidth * s);

  const x0 = Math.max(0, Math.floor(cx - boundR - 3));
  const y0 = Math.max(0, Math.floor(cy - boundR - 3));
  const x1 = Math.min(W, Math.ceil(cx + boundR + 3));
  const y1 = Math.min(H, Math.ceil(cy + boundR + 3));
  const bw = x1 - x0;
  const bh = y1 - y0;
  if (bw <= 0 || bh <= 0) return true;

  const hasSnapshot = snapshotCanvas && snapshotCtx && snapshotCanvas.width >= W * 0.8 && snapshotCanvas.height >= H * 0.8;
  if (!hasSnapshot) {
    drawShadowOnly(ctx, ripple, age, progress);
    return true;
  }

  let srcImage;
  try {
    srcImage = snapshotCtx.getImageData(0, 0, snapshotCanvas.width, snapshotCanvas.height);
  } catch (error) {
    drawShadowOnly(ctx, ripple, age, progress);
    return true;
  }

  const src = srcImage.data;
  const srcW = snapshotCanvas.width;
  const srcH = snapshotCanvas.height;
  const dest = ctx.createImageData(bw, bh);
  const out = dest.data;

  const k = (Math.PI * 2) / (waterSettings.wavelength * s);
  const band = waterSettings.ringWidth * s;
  const fade = Math.pow(1 - progress, 1.7);
  const strength = waterSettings.strength * s * fade;
  const shadowStrength = waterSettings.shadowStrength * fade;
  const sourceScaleX = srcW / W;
  const sourceScaleY = srcH / H;

  for (let py = 0; py < bh; py += 1) {
    const y = y0 + py;
    const dy = y - cy;
    for (let px = 0; px < bw; px += 1) {
      const x = x0 + px;
      const dx = x - cx;
      const r = Math.sqrt(dx * dx + dy * dy);
      const outIndex = (py * bw + px) * 4;

      if (r < innerR || r > boundR || r < 1) {
        out[outIndex + 3] = 0;
        continue;
      }

      const phase = r - front;
      let wave = 0;
      let slope = 0;
      let opacity = 0;

      // Main outgoing capillary-like wave plus trailing smaller rings.
      for (let n = 0; n < waterSettings.tailRings; n += 1) {
        const center = -n * waterSettings.wavelength * s * 0.95;
        const local = phase - center;
        const envelope = Math.exp(-(local * local) / (2 * band * band));
        const ringDecay = Math.pow(0.64, n);
        const localPhase = local * k;
        wave += Math.sin(localPhase) * envelope * ringDecay;
        slope += Math.cos(localPhase) * envelope * ringDecay;
        opacity += envelope * ringDecay;
      }

      // Soft circular cutoff, so no object suddenly appears/disappears.
      const radialFadeIn = smoothstep(innerR, innerR + 18 * s, r);
      const radialFadeOut = 1 - smoothstep(boundR - 34 * s, boundR, r);
      const localAlpha = clamp(opacity * 0.72 * fade * radialFadeIn * radialFadeOut, 0, 0.82);
      if (localAlpha <= 0.004) {
        out[outIndex + 3] = 0;
        continue;
      }

      const nx = dx / r;
      const ny = dy / r;

      // Displacement follows the radial gradient of the wave height.
      const displacement = strength * slope;
      const swirl = 0.9 * s * wave * fade;
      const sampleX = (x - nx * displacement - ny * swirl) * sourceScaleX;
      const sampleY = (y - ny * displacement + nx * swirl) * sourceScaleY;
      const sampled = sampleBilinear(src, srcW, srcH, sampleX, sampleY);

      // Grey concavity/convexity shading. Mostly dark grey shadows; highlights
      // are deliberately muted to avoid white drawn rings.
      const grey = 120;
      const shade = clamp(slope * shadowStrength, -0.42, 0.28);
      let rr = sampled[0];
      let gg = sampled[1];
      let bb = sampled[2];

      if (shade < 0) {
        const dark = -shade;
        rr = rr * (1 - dark) + grey * dark;
        gg = gg * (1 - dark) + grey * dark;
        bb = bb * (1 - dark) + grey * dark;
      } else {
        const light = shade * 0.45;
        rr = rr * (1 - light) + 185 * light;
        gg = gg * (1 - light) + 192 * light;
        bb = bb * (1 - light) + 198 * light;
      }

      out[outIndex] = rr;
      out[outIndex + 1] = gg;
      out[outIndex + 2] = bb;
      out[outIndex + 3] = clamp(localAlpha * 255, 0, 210);
    }
  }

  ctx.putImageData(dest, x0, y0);
  return true;
}

function animateWater(now) {
  if (!waterCanvas || !waterCtx) return;
  resizeWaterCanvas();
  waterCtx.clearRect(0, 0, waterCanvas.width, waterCanvas.height);

  ripples = ripples.filter((ripple) => drawRefractedRipple(waterCtx, ripple, now));

  if (ripples.length > 0) {
    waterCanvas.classList.add("is-active");
    requestAnimationFrame(animateWater);
  } else {
    waterCanvas.classList.remove("is-active");
    animationRunning = false;
    waterCtx.clearRect(0, 0, waterCanvas.width, waterCanvas.height);
  }
}


function startWaterRippleAt(clientX, clientY) {
  ripples.push({
    x: clientX,
    y: clientY,
    start: performance.now()
  });

  if (!animationRunning) {
    animationRunning = true;
    requestAnimationFrame(animateWater);
  }
}

async function addWaterRipple(event) {
  if (!waterCanvas || !waterCtx) return;
  if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return;

  // Do not trigger water on modified clicks intended to open links in new tabs.
  if (event.metaKey || event.ctrlKey || event.shiftKey || event.altKey) return;

  resizeWaterCanvas();

  const key = viewportKey();
  if (!snapshotCanvas || snapshotKey !== key) {
    await captureViewport();
  }

  startWaterRippleAt(event.clientX, event.clientY);
}

// Keep a reasonably fresh snapshot. This makes ordinary clicks immediate,
// while still allowing scroll/language changes to be reflected.
function scheduleSnapshot(delay = 260) {
  markSnapshotStale();
  window.clearTimeout(scrollTimer);
  scrollTimer = window.setTimeout(() => {
    if (document.visibilityState === "visible") captureViewport();
  }, delay);
}

resizeWaterCanvas();
window.addEventListener("load", () => {
  window.setTimeout(async () => {
    await captureViewport();
    // A gentle opening ripple, inspired by sites that begin with a water-surface motion.
    startWaterRippleAt(window.innerWidth * 0.5, window.innerHeight * 0.43);
  }, 520);
});
window.addEventListener("resize", () => {
  window.clearTimeout(resizeTimer);
  resizeTimer = window.setTimeout(() => {
    resizeWaterCanvas();
    scheduleSnapshot(220);
  }, 160);
});
window.addEventListener("scroll", () => scheduleSnapshot(180), { passive: true });
document.addEventListener("visibilitychange", () => {
  if (document.visibilityState === "visible") scheduleSnapshot(180);
});

// Update the snapshot after language/reveal changes have altered the text.
buttons.forEach((button) => {
  button.addEventListener("click", () => window.setTimeout(() => scheduleSnapshot(80), 90));
});

// Reveal animations change the page appearance, so refresh the snapshot after
// the initial entrance animations are likely finished.
window.setTimeout(() => scheduleSnapshot(120), 1200);

document.addEventListener("click", addWaterRipple, { passive: true });
